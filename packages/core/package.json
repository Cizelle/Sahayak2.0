{
	"name": "@adaptivemesh/core",
	"version": "0.1.0",
	"description": "Pure-TypeScript AdaptiveMesh protocol engine: identity, crypto, envelope, framing, routing, dedup, TTL, congestion, store-and-forward, persistence. No platform dependencies.",
	"type": "module",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"test": "node --import tsx --test \"test/**/*.test.ts\"",
		"test:tap": "node --import tsx --test --test-reporter tap \"test/**/*.test.ts\"",
		"typecheck": "tsc -p tsconfig.json --noEmit"
	},
	"license": "Apache-2.0"
}
